name: Release Builds

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Type of release'
        required: true
        type: choice
        options:
          - 'rc'
          - 'final'
        default: 'rc'
      version:
        description: 'Version (e.g., 0.1.0 for RC or final release)'
        required: true
        default: '0.1.0'
      rc_number:
        description: 'RC number (only for RC builds, e.g., 1 for rc.1)'
        required: false
        default: '1'
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  validate-inputs:
    name: Validate Release Inputs
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    outputs:
      release_version: ${{ steps.version.outputs.release_version }}
      is_prerelease: ${{ steps.version.outputs.is_prerelease }}
      
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('release/{0}', inputs.version) || github.ref }}

      - name: Determine release version
        id: version
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            # Extract version from tag (remove 'v' prefix)
            VERSION=${GITHUB_REF#refs/tags/v}
            echo "release_version=${VERSION}" >> $GITHUB_OUTPUT
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
          else
            # Manual workflow dispatch
            if [[ "${{ github.event.inputs.release_type }}" == "rc" ]]; then
              VERSION="${{ github.event.inputs.version }}-rc.${{ github.event.inputs.rc_number }}"
              echo "release_version=${VERSION}" >> $GITHUB_OUTPUT
              echo "is_prerelease=true" >> $GITHUB_OUTPUT
            else
              VERSION="${{ github.event.inputs.version }}"
              echo "release_version=${VERSION}" >> $GITHUB_OUTPUT
              echo "is_prerelease=false" >> $GITHUB_OUTPUT
            fi
          fi
          echo "Release version: ${VERSION}"

      - name: Verify release branch exists
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        run: |
          BRANCH="release/${{ github.event.inputs.version }}"
          if ! git show-ref --verify --quiet refs/remotes/origin/$BRANCH; then
            echo "❌ Release branch $BRANCH does not exist!"
            echo "Please create the release branch first:"
            echo "  git checkout develop"
            echo "  git checkout -b $BRANCH"
            echo "  git push origin $BRANCH"
            exit 1
          fi
          echo "✅ Release branch $BRANCH exists"

  build:
    name: Build and Package (${{ matrix.artifact_name }})
    needs: [validate-inputs]
    if: always() && (needs.validate-inputs.result == 'success' || github.event_name == 'push')
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-20.04 # Keep specific ubuntu for musl if needed, or use ubuntu-latest
            lld_name: ld.lld
            artifact_name: x86_64-unknown-linux-musl
            target: x86_64-unknown-linux-musl
          - os: macos-latest
            lld_name: ld.lld
            artifact_name: aarch64-apple-darwin
            target: aarch64-apple-darwin
          - os: windows-latest
            lld_name: ld.lld.exe # Note: will be copied as rust-lld.exe in package
            artifact_name: x86_64-pc-windows-msvc
            target: x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('release/{0}', github.event.inputs.version) || github.ref }}

      - name: Install Rust
        uses: dtolnay/rust-toolchain@latest

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}

      - name: Get release version
        id: release_version
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
          else
            VERSION="${{ needs.validate-inputs.outputs.release_version }}"
          fi
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Release version: ${VERSION}"

      - name: Update version in Cargo.toml
        shell: bash
        run: |
          # Temporarily update version for release build
          sed -i.bak 's/^version = ".*"/version = "${{ steps.release_version.outputs.version }}"/' Cargo.toml

      - name: Run tests
        run: cargo test

      - name: Install Rust target
        shell: bash # Ensure this runs in bash for all OSs, especially Windows
        run: |
          if [[ -n "${{ matrix.target }}" ]]; then
            rustup target add ${{ matrix.target }}
          else
            echo "No specific target in matrix, assuming native build."
          fi

      - name: Build bam
        shell: bash # Ensure this runs in bash for all OSs, especially Windows
        env:
          RUSTFLAGS: ${{ (matrix.target == 'x86_64-unknown-linux-musl' && '-C target-feature=+crt-static') || '' }}
        run: |
          if [[ -n "${{ matrix.target }}" ]]; then
            cargo build --release --target ${{ matrix.target }}
          else
            cargo build --release
          fi

      - name: Determine Bam Executable Name and Path
        id: bam_exe
        shell: bash
        run: |
          BAM_EXE_NAME="bam"
          [[ "${{ matrix.os }}" == "windows-latest" ]] && BAM_EXE_NAME="bam.exe"
          echo "bam_exe_name=$BAM_EXE_NAME" >> $GITHUB_OUTPUT

          if [[ -n "${{ matrix.target }}" ]]; then
            SOURCE_BAM_PATH="target/${{ matrix.target }}/release/$BAM_EXE_NAME"
          else
            SOURCE_BAM_PATH="target/release/$BAM_EXE_NAME"
          fi
          echo "source_bam_path=$SOURCE_BAM_PATH" >> $GITHUB_OUTPUT

      - name: Setup LLD Download Information
        id: lld_info
        shell: bash
        run: |
          LLVM_EXTRACT_DIR="llvm_downloaded"
          mkdir -p "$LLVM_EXTRACT_DIR"
          echo "LLVM_EXTRACT_DIR=$LLVM_EXTRACT_DIR" >> $GITHUB_ENV

          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            echo "LLVM_VERSION_ID=llvmorg-20.1.1" >> $GITHUB_ENV
            echo "LLVM_TARBALL_NAME=clang+llvm-20.1.1-x86_64-pc-windows-msvc.tar.xz" >> $GITHUB_ENV
            echo "LLVM_DOWNLOAD_URL=https://github.com/llvm/llvm-project/releases/download/llvmorg-20.1.1/clang+llvm-20.1.1-x86_64-pc-windows-msvc.tar.xz" >> $GITHUB_ENV
            echo "LLVM_BIN_SUBPATH=clang+llvm-20.1.1-x86_64-pc-windows-msvc/bin" >> $GITHUB_ENV
            echo "EXTRACTED_LLD_NAME=lld.exe" >> $GITHUB_ENV
            echo "PACKAGED_LLD_NAME=lld.exe" >> $GITHUB_ENV
          elif [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            echo "LLVM_VERSION_ID=llvmorg-20.1.1" >> $GITHUB_ENV
            echo "LLVM_TARBALL_NAME=LLVM-20.1.1-macOS-ARM64.tar.xz" >> $GITHUB_ENV
            echo "LLVM_DOWNLOAD_URL=https://github.com/llvm/llvm-project/releases/download/llvmorg-20.1.1/LLVM-20.1.1-macOS-ARM64.tar.xz" >> $GITHUB_ENV
            echo "LLVM_BIN_SUBPATH=LLVM-20.1.1-macOS-ARM64/bin" >> $GITHUB_ENV
            echo "EXTRACTED_LLD_NAME=lld" >> $GITHUB_ENV
            echo "PACKAGED_LLD_NAME=lld" >> $GITHUB_ENV
          elif [[ "${{ matrix.os }}" == "ubuntu-20.04" ]]; then # Linux
            echo "LLVM_VERSION_ID=static-clang-v20.1.1" >> $GITHUB_ENV
            echo "LLVM_TARBALL_NAME=linux_amd64.tar.xz" >> $GITHUB_ENV
            echo "LLVM_DOWNLOAD_URL=https://github.com/dzbarsky/static-clang/releases/download/v20.1.1/linux_amd64.tar.xz" >> $GITHUB_ENV
            echo "LLVM_BIN_SUBPATH=bin" >> $GITHUB_ENV # Assumes bin/lld is at top level after extraction
            echo "EXTRACTED_LLD_NAME=lld" >> $GITHUB_ENV
            echo "PACKAGED_LLD_NAME=ld.lld" >> $GITHUB_ENV # Specific name for Linux as per request
          fi

      - name: Cache LLVM Tarball
        uses: actions/cache@v4
        id: cache_llvm_tarball
        with:
          path: ./${{ env.LLVM_TARBALL_NAME }}
          key: ${{ runner.os }}-${{ env.LLVM_VERSION_ID }}-${{ env.LLVM_TARBALL_NAME }}

      - name: Download LLVM Tarball
        if: steps.cache_llvm_tarball.outputs.cache-hit != 'true'
        shell: bash
        run: |
          echo "Downloading ${{ env.LLVM_DOWNLOAD_URL }} to ${{ env.LLVM_TARBALL_NAME }}"
          curl -L -o "${{ env.LLVM_TARBALL_NAME }}" "${{ env.LLVM_DOWNLOAD_URL }}"

      - name: Extract and Package LLD
        shell: bash
        run: |
          mkdir -p package
          echo "Extracting ${{ env.LLVM_TARBALL_NAME }} into ${{ env.LLVM_EXTRACT_DIR }}"
          tar -xf "${{ env.LLVM_TARBALL_NAME }}" -C "${{ env.LLVM_EXTRACT_DIR }}"

          SOURCE_LLD_PATH="${{ env.LLVM_EXTRACT_DIR }}/${{ env.LLVM_BIN_SUBPATH }}/${{ env.EXTRACTED_LLD_NAME }}"
          DEST_LLD_PATH="package/${{ env.PACKAGED_LLD_NAME }}"
          
          echo "Copying LLD from ${SOURCE_LLD_PATH} to ${DEST_LLD_PATH}"
          cp "${SOURCE_LLD_PATH}" "${DEST_LLD_PATH}"
          chmod +x "${DEST_LLD_PATH}"

          echo "Copying bam executable to package"
          cp "${{ steps.bam_exe.outputs.source_bam_path }}" "package/${{ steps.bam_exe.outputs.bam_exe_name }}"
          
          echo "Package contents:"
          ls -la package/

          if [[ "${{ matrix.os }}" == 'macos-latest' ]]; then
            echo "Verifying dynamic dependencies of packaged LLD on macOS:"
            otool -L "package/${{ env.PACKAGED_LLD_NAME }}" # Should be package/lld
          fi

      - name: Test compilation end-to-end
        shell: bash
        run: |
          cd package
          
          # Create test program
          echo 'print(message: "Hello from release build!")' > test.bam
          
          # Test compilation
          TEST_OUTPUT_EXE="test"
          [[ "${{ matrix.os }}" == "windows-latest" ]] && TEST_OUTPUT_EXE="test.exe"

          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            ./${{ steps.bam_exe.outputs.bam_exe_name }} test.bam
            ./$TEST_OUTPUT_EXE
          else
            ./${{ steps.bam_exe.outputs.bam_exe_name }} test.bam
            ./$TEST_OUTPUT_EXE
          fi

      - name: Create package archive
        shell: bash
        run: |
          cd package
          
          # Create platform-specific archive
          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            # Create ZIP for Windows
            7z a "../bam-${{ steps.release_version.outputs.version }}-${{ matrix.artifact_name }}.zip" .
          else
            # Create tar.gz for Unix-like systems
            tar -czf "../bam-${{ steps.release_version.outputs.version }}-${{ matrix.artifact_name }}.tar.gz" .
          fi
          
          cd ..
          echo "Created package:"
          ls -la bam-${{ steps.release_version.outputs.version }}-${{ matrix.artifact_name }}.*

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.artifact_name }}-${{ steps.release_version.outputs.version }}
          path: |
            bam-${{ steps.release_version.outputs.version }}-${{ matrix.artifact_name }}.tar.gz
            bam-${{ steps.release_version.outputs.version }}-${{ matrix.artifact_name }}.zip
          if-no-files-found: ignore
          retention-days: 90

      - name: Restore Cargo.toml
        shell: bash
        run: |
          # Restore original Cargo.toml
          mv Cargo.toml.bak Cargo.toml

  release:
    name: Create GitHub Release
    needs: [validate-inputs, build]
    runs-on: ubuntu-latest
    if: always() && needs.build.result == 'success' && (startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch')
    
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('release/{0}', github.event.inputs.version) || github.ref }}

      - name: Get release details
        id: release_details
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
            IS_PRERELEASE=false
          else
            VERSION="${{ needs.validate-inputs.outputs.release_version }}"
            IS_PRERELEASE="${{ needs.validate-inputs.outputs.is_prerelease }}"
          fi
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "is_prerelease=${IS_PRERELEASE}" >> $GITHUB_OUTPUT
          
          # Generate release type label
          if [[ "$VERSION" == *"-rc."* ]]; then
            echo "type_label=Release Candidate" >> $GITHUB_OUTPUT
            echo "type_emoji=🧪" >> $GITHUB_OUTPUT
          elif [[ "$IS_PRERELEASE" == "true" ]]; then
            echo "type_label=Pre-release" >> $GITHUB_OUTPUT
            echo "type_emoji=⚠️" >> $GITHUB_OUTPUT
          else
            echo "type_label=Stable Release" >> $GITHUB_OUTPUT
            echo "type_emoji=🚀" >> $GITHUB_OUTPUT
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.release_details.outputs.version }}
          release_name: ${{ steps.release_details.outputs.type_emoji }} bam ${{ steps.release_details.outputs.version }} (${{ steps.release_details.outputs.type_label }})
          body: |
            ## bam ${{ steps.release_details.outputs.version }} (${{ steps.release_details.outputs.type_label }})
            
            ${{ steps.release_details.outputs.is_prerelease == 'true' && '**⚠️ This is a pre-release version for testing purposes.**' || 'Stable release of the bam programming language compiler.' }}
            
            Standalone bam compiler with bundled LLD linker.
            
            ### Installation
            
            1. Download the appropriate package for your platform:
               - **Linux (x86_64 musl)**: `bam-${{ steps.release_details.outputs.version }}-x86_64-unknown-linux-musl.tar.gz`
               - **macOS (Apple Silicon, arm64)**: `bam-${{ steps.release_details.outputs.version }}-aarch64-apple-darwin.tar.gz`
               - **Windows (x86_64 msvc)**: `bam-${{ steps.release_details.outputs.version }}-x86_64-pc-windows-msvc.zip`
            
            2. Extract the package:
               ```bash
               # Linux/macOS
               tar -xzf bam-${{ steps.release_details.outputs.version }}-*.tar.gz
               
               # Windows
               # Extract the .zip file using Windows Explorer or:
               # Expand-Archive bam-${{ steps.release_details.outputs.version }}-*.zip
               ```
            
            3. Test the installation:
               ```bash
               # Create a simple hello world program
               echo 'print(message: "Hello, World!")' > hello.bam
               
               # Compile and run
               ./bam hello.bam
               ./hello
               ```
            
            ### What's Included
            
            - **`bam`** - The bam compiler
            - **`ld.lld`** - Bundled LLD linker (no external dependencies needed)
            
            ### System Requirements
            
            - **No external dependencies required**
            - Works on base OS installations (Linux, macOS, Windows)
            - No need to install Rust, LLVM, or other development tools
            
            ${{ steps.release_details.outputs.is_prerelease == 'true' && '### Testing and Feedback

            This is a pre-release build. Please:
            - Test thoroughly before using in production
            - Report any issues on [GitHub Issues](https://github.com/' || '### Support

            For questions and issues, please visit [GitHub Issues](https://github.com/' }}${{ github.repository }}/issues)
            ${{ steps.release_details.outputs.is_prerelease == 'true' && '- Provide feedback on new features and changes' || '' }}
            
            ### License
            
            This distribution includes LLD from the LLVM Project.
            LLD is licensed under the Apache License 2.0 with LLVM Exceptions.
            
            ---
            
            **Build Info:**
            - **Commit**: ${{ github.sha }}
            - **Branch**: ${{ github.event_name == 'workflow_dispatch' && format('release/{0}', github.event.inputs.version) || github.ref_name }}
            - **Build Date**: ${{ github.run_id }}
          draft: false
          prerelease: ${{ steps.release_details.outputs.is_prerelease == 'true' }}

      - name: Upload release assets
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.release_details.outputs.version }}"
          UPLOAD_URL="${{ steps.create_release.outputs.upload_url }}"
          
          # Function to upload a file
          upload_asset() {
            local file_path="$1"
            local asset_name="$2"
            local content_type="$3"
            
            if [[ -f "$file_path" ]]; then
              echo "Uploading $asset_name..."
              curl -s -H "Authorization: token $GITHUB_TOKEN" \
                -H "Content-Type: $content_type" \
                --data-binary @"$file_path" \
                "${UPLOAD_URL%\{*}?name=${asset_name}"
              echo "✅ Uploaded $asset_name"
            else
              echo "❌ File not found: $file_path"
            fi
          }
          
          # Upload Linux package
          upload_asset \
            "./artifacts/release-x86_64-unknown-linux-musl-${VERSION}/bam-${VERSION}-x86_64-unknown-linux-musl.tar.gz" \
            "bam-${VERSION}-x86_64-unknown-linux-musl.tar.gz" \
            "application/gzip"
          
          # Upload macOS ARM package
          upload_asset \
            "./artifacts/release-aarch64-apple-darwin-${VERSION}/bam-${VERSION}-aarch64-apple-darwin.tar.gz" \
            "bam-${VERSION}-aarch64-apple-darwin.tar.gz" \
            "application/gzip"
          
          # Upload Windows package
          upload_asset \
            "./artifacts/release-x86_64-pc-windows-msvc-${VERSION}/bam-${VERSION}-x86_64-pc-windows-msvc.zip" \
            "bam-${VERSION}-x86_64-pc-windows-msvc.zip" \
            "application/zip"

      - name: Release summary
        shell: bash
        run: |
          VERSION="${{ steps.release_details.outputs.version }}"
          TYPE_LABEL="${{ steps.release_details.outputs.type_label }}"
          TYPE_EMOJI="${{ steps.release_details.outputs.type_emoji }}"
          
          echo "$TYPE_EMOJI bam $VERSION ($TYPE_LABEL) published successfully!"
          echo "🔗 Release URL: https://github.com/${{ github.repository }}/releases/tag/v${VERSION}"
          
          if [[ "${{ steps.release_details.outputs.is_prerelease }}" == "true" ]]; then
            echo "📋 This is a pre-release - please test thoroughly before production use"
          else
            echo "🎉 This is a stable release ready for production use"
          fi